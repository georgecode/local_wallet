////Generates key pairs
// let genKeyPairs = web3.eth.accounts.create();
// console.log("KEYS", genKeyPairs);



const genKeys = (coin = "BTC") => {
	return "genKey components"
}

export default genKeys;
